# Minimal requirements for running the notebooks and scripts in this workspace
# Use a virtual environment and install the file with: pip install -r requirements.txt

pandas>=1.5,<2.0
numpy>=1.23,<2.0
matplotlib>=3.5,<4.0
seaborn>=0.12,<1.0
scikit-learn>=1.2,<2.0
scipy>=1.8,<2.0

# Jupyter / notebook runtime
jupyterlab>=3.6
notebook>=6.5
ipykernel>=6.0

# Utilities used by sklearn & parallel processing
joblib>=1.2
threadpoolctl>=3.1
